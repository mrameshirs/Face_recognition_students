# Minimal Requirements for Streamlit Cloud
# This avoids dlib compilation issues

streamlit==1.31.1
dropbox==12.0.2
pandas==2.2.1
openpyxl==3.1.2
pillow==10.2.0
cryptography==42.0.5
xlsxwriter==3.2.0

# Use opencv-python-headless (lighter, no GUI dependencies)
opencv-python-headless==4.6.0.66

# Face recognition alternative that works better on cloud
# Option 1: Use deepface (easier to install)
deepface==0.0.79

# Option 2: Or if you must use face_recognition, pin exact versions
# face-recognition==1.3.0
# face-recognition-models==0.3.0
